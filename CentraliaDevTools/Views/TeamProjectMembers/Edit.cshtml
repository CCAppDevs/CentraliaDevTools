@model CentraliaDevTools.Models.TeamProjectMember

@{
    ViewData["Title"] = "Edit TeamProjectMember";
}

<h1>Edit</h1>

<h4>TeamProjectMember</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TeamProjectMemberID" />
            <div class="form-group">
                <label asp-for="TeamProjectId" class="control-label"></label>
                <select asp-for="TeamProjectId" class="form-control" asp-items="ViewBag.TeamProjectId"></select>
                <span asp-validation-for="TeamProjectId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="MemberId" class="control-label"></label>
                <select asp-for="MemberId" class="form-control" asp-items="ViewBag.MemberId"></select>
                <span asp-validation-for="MemberId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
